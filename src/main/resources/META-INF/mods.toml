modLoader="javafml" #mandatory
loaderVersion="[31,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
issueTrackerURL="http://my.issue.tracker/" #optional
license = "MIT"
[[mods]] #mandatory

modId="minorconvenience" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Minor Convenience" #mandatory
displayURL="https://my.url.me" #optional

logoFile="minorconvenience.png" #optional
credits="Thanks to Forge for its amazing API, those on its discord, and specifically gigaherz for having great mods to check code with." #optional
authors="Fealtous" #optional
description='''
This mod is simply a collection of minor conveniences, specifically geared towards Hypixel Skyblock.
'''

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.minorconvenience]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="CLIENT"
# Here's another dependency
[[dependencies.minorconvenience]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.16.5]"
    ordering="NONE"
    side="CLIENT"
